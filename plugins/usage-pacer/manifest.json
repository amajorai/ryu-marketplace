{
  "id": "@ryu/usage-pacer",
  "name": "Usage Pacer",
  "version": "0.1.15",
  "stability": "experimental",
  "category": "Automation",
  "description": "Keeps subscription agents usable for the whole rolling window. It reads each agent's 5-hour and weekly usage, calculates deficit or reserve against an even burn rate, and selects the next model in your configured fallback ladder before a turn starts.",
  "tagline": "Spend subscription quota at a sustainable pace",
  "keywords": ["usage", "quota", "models", "automation", "subscription"],
  "icon": "gauge-02",
  "iconDither": { "from": 32, "to": "transparent", "direction": "down" },
  "surfaces": { "gateway": { "support": "none" }, "core": { "support": "full" }, "desktop": { "support": "full" }, "island": { "support": "limited" }, "mobile": { "support": "limited" }, "extension": { "support": "limited" }, "web": { "support": "full" }, "cli": { "support": "full" } },
  "engines": { "ryu": ">=0.1.0" },
  "runnables": [],
  "permission_grants": ["preferences:read", "usage:read"],
  "activation_events": ["*"],
  "contributes": {
    "settings_tabs": [{
      "id": "usage-pacer.settings",
      "title": "Usage pacing",
      "scope": "node",
      "fields": [
        { "type": "toggle", "pref_key": "usage-pacer-enabled", "label": "Automatically select lower-cost models", "description": "Applies your ladder only to agents with readable subscription usage windows. It never changes an agent's saved model.", "default": false },
        { "type": "select", "pref_key": "usage-pacer-mode", "label": "Pacing mode", "description": "Even burn compares usage with the remaining-window pace and applies extra pressure when you are in deficit. Usage thresholds only switches when an explicit ladder threshold is crossed.", "default": "even", "options": [{ "value": "even", "label": "Even burn (recommended)" }, { "value": "threshold", "label": "Usage thresholds only" }] },
        { "type": "textarea", "pref_key": "usage-pacer-rules", "label": "Agent and global ladders", "description": "JSON configuration. Agent entries override global entries. `windows` accepts `weekly`, `session`, or `both`; `at` is used-percent. In even mode, deficit beyond `pace_deficit` adds pressure toward the next rung.", "default": "{\n  \"global\": {\n    \"windows\": \"weekly\",\n    \"pace_deficit\": 15,\n    \"ladder\": [\n      { \"from\": \"opus\", \"at\": 50, \"model\": \"sonnet\" },\n      { \"from\": \"sonnet\", \"at\": 75, \"model\": \"haiku\" }\n    ]\n  },\n  \"agents\": {\n    \"acp:claude\": {\n      \"windows\": \"both\",\n      \"pace_deficit\": 15,\n      \"ladder\": [\n        { \"from\": \"opus\", \"at\": 50, \"model\": \"sonnet\" },\n        { \"from\": \"sonnet\", \"at\": 75, \"model\": \"haiku\" }\n      ]\n    }\n  }" }
      ]
    }],
    "turn_hooks": [{ "id": "usage-pacer.select", "on": "pre_model_select", "code_file": "hooks/select-model.js" }]
  }
}
