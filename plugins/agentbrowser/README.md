# Agent Browser

A Ryu plugin.

Declarative Ryu plugin (no UI, no Core Rust). Definition lives in `manifest.json`; a byte-identical copy is registered built-in in Core (`apps/core/src/plugin_manifest/fixtures/agentbrowser.manifest.json`). Published to the grouped `ryu-marketplace` via `tools/mirror-plugins.sh`.
