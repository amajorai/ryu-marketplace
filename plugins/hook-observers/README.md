# Hook Observers

A Ryu plugin.

Declarative Ryu plugin (no UI, no Core Rust). Definition lives in `plugin.json`; a byte-identical copy is registered built-in in Core (`apps/core/src/plugin_manifest/fixtures/hook-observers.plugin.json`). Published to the grouped `ryu-marketplace` via `tools/mirror-plugins.sh`.
